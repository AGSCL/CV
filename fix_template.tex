\makeatletter
\renewcommand{\@seccntformat}[1]{}
\let\originalsection\section
\renewcommand{\section}[1]{%
  \ifnum\value{section}<5\relax
    \originalsection{#1}
  \else
    % Skip sections with numbers >= 5
  \fi
}
\makeatother